apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: smartcluster
  labels:
    app: api-gateway
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: api-gateway
  type: ClusterIP
